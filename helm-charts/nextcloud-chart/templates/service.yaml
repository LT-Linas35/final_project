apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: nextcloud
spec:
  type: LoadBalancer
  sessionAffinity: ClientIP
  selector:
    app: {{ .Release.Name }}
  ports:
    - port: 80
      targetPort: 80
