apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: nextcloud
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}
    role: active
  ports:
  - port: 80
    targetPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-preview
  namespace: nextcloud
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}
    role: active
  ports:
  - port:  {{ .Values.service.port }}
    targetPort: 80
